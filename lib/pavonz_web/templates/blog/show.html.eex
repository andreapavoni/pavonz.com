<section>
    <header><h4><%= link "← All posts", to: Routes.blog_path(@conn, :index)%></h4></header>
	  <article id="<%= @post.id %>">
      <header>
        <time><small><%= @post.date %></small></time>
        <h2><%= link @post.title, to: Routes.blog_path(@conn, :show, @post)%></h2>
      </header>

      <%= raw @post.body %>



      <footer><h5>tags: <%= Enum.join(@post.tags, ", ") %></h5></footer>
	  </article>
</section>
